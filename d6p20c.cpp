#include <bits/stdc++.h>

int main()
{
    long double r,a;
    scanf("%Lf",&r);            //%Lf for long double
    a = 3.141592653*pow(r,2);
    printf("%.9Lf",a);          //.9 for no. of numbers after decimal
}